#include <stdio.h>
/**
 * main -Entry point
 *
 * Return: Aways 0 (Success)
 */
int main(void)
{
        int n;
	char z = 'z';

	for (n == 26;n > 0; n--)
	{
		putchar(z);
        }
	putchar('\n');
	return (0);
}
