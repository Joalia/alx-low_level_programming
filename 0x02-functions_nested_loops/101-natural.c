#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i, som;
	for (i = 0; i < 1025; i++)
	{
		if ( i % 3 = 0)
			som = som + i;
		else if ( i % 5)
			som = som +i;
	}
	printf("%d\n", som);
	return (0);
}
